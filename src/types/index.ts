export type TContact = {
    firstName: string;
    lastName: string;
    phone: string;
    email: string;
    subject: string;
    message: string;
};
export type TError = {
    message: string
}
export type TMessage = {
    name: string;
    email: string;
    phone: string;
    subject: string;
    message: string;
};